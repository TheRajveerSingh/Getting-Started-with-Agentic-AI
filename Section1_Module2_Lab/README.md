1. The "Study Material" file is just the mateiral provided by coursera. It is for learning, understanding, and implementing them in our own way.
2. The "01_ChatModels&Message" file shows how to use LLMs to respond to our answers, but in the way we want, by giving it some 'context' to our problem and instructions. This is done by first convering the LLM into a chat model (by wrapping), and then using Chat Messages (like human, ai, system) we can invoke the answer generated by the llm, by using our own context.
3. The "02_ComparingModels" file is where I practiced 
-> how to define models (llama, granite), 
-> then how to define the types of parameters we would like to use (creative/precise by tuning temperature), 
-> then creating instances for those models, by (model X parameter_type), so here we had 4 of them, since we were working with 2 types of models(llama & granite), and two types of parameters (creative & precise),
-> then We took the same prompt, and understood how responses differ from model to model, and how parameters also effect the type of responses generated from those models
-> Conculsions are written at the end of the lab
...........
Just for myself, I am noting this down, as it was creating confusion for me:
Watsonxai: IBM's platform
llama: Meta's Model. This AI is being hosted by IBM on their watsonx.ai platform
WatsonxLLM: This is a connector class in python that lets us talk with whatever model in running on Watsonx.ai.
...........